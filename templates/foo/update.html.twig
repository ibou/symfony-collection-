{% extends 'base.html.twig' %}

{% block title %}Update {% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                {{ form_start(form) }}
                {{ include("foo/_form.html.twig", {form: form}) }}
                <button type="submit" class="btn btn-success">Update</button>
                {{ form_end(form, {render_rest: false}) }}
            </div>
        </div>
    </div>

{% endblock %}
